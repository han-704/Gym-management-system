# 健身房管理系统 — 性能测试用例

> **项目名称**：健身房管理系统  
> **技术架构**：Spring Boot + Vue.js + MySQL + 智谱AI GLM-4-Flash  
> **测试环境**：4核8G服务器，MySQL 8.0，JDK 1.8+，局域网1000Mbps  
> **测试工具**：JMeter、VisualVM、Lighthouse、Postman  
> **文档版本**：v1.0  

---

## 一、响应时间测试

---

**用例编号**  XT-01
**用例名称**  会员资料分页查询响应时间测试
**测试目的**  验证系统对常规GET请求的响应时间是否满足性能准则要求（<2秒），以会员资料分页查询接口 `GET /huiyuan/page` 作为典型代表
**测试方法**  使用JMeter单线程组发送20次请求，记录每次响应时间，取平均值和P95值；同时使用浏览器DevTools Network面板观察前端页面从发起请求到渲染完成的完整耗时
**测试数据**  请求参数：`page=1&limit=10`，数据库会员表预置100条数据
**预期结果**  后端API平均响应时间 < 2000ms，P95响应时间 < 2000ms，前端页面数据渲染完成总耗时 < 2500ms
**实际结果**  

---

## 测试结论（响应时间测试）

测试结果表明，`GET /huiyuan/page` 接口的平均响应时间为______ms，P95响应时间为______ms，______于2秒的性能准则要求。

---

## 二、并发性能测试

---

**用例编号**  BF-01
**用例名称**  100并发用户混合业务负载测试
**测试目的**  验证系统在100个并发用户同时在线操作时，Tomcat线程池能否正常调度，请求不被阻塞，满足性能准则中"支持至少100个并发用户"的要求
**测试方法**  使用JMeter设置线程组：100个线程，Ramp-Up周期20秒，持续运行30分钟；混合业务场景按比例模拟真实施压：会员查询（30%）、器材列表浏览（25%）、心得社区发帖（20%）、AI教练问答（15%）、健身活动管理（10%）。实时监控Tomcat线程池状态、错误率和响应时间
**测试数据**  数据库预置200条会员、100条器材、50条心得帖子、15条AI知识库问答对；100个不同账号交替操作
**预期结果**  请求成功率 ≥ 99.5%，平均响应时间 < 3000ms，P95响应时间 < 5000ms，Tomcat线程池活跃线程数在合理范围（50-180之间），无请求被长时间阻塞（阻塞时间 < 10秒），无"Connection refused"或线程池耗尽异常
**实际结果**  

---

## 测试结论（并发性能测试）

100并发用户混合业务负载下，请求成功率为______%，平均响应时间为______ms，Tomcat线程池______正常调度，______出现请求被阻塞的情况。

---

## 三、AI故障转移测试

---

**用例编号**  GZ-01
**用例名称**  GLM-4-Flash API超时故障转移测试
**测试目的**  验证当智谱AI API调用超时（连接超时30秒或读取超时120秒）时，系统能否秒级切换至规则引擎生成备用健身计划，并向前端返回友好提示，保证核心业务逻辑不中断
**测试方法**  使用网络模拟工具（Charles/Fiddler）对智谱AI API域名（open.bigmodel.cn）设置连接延迟60000ms（60秒，超过30秒连接超时），触发后端超时机制；在前端 `plan-generate.vue` 页面点击"生成计划"，观察前端加载状态、后端降级日志和最终返回的计划内容。同时检查数据库中 `fitness_plan_detail` 表写入的是规则引擎模板数据
**测试数据**  输入参数：`{"huiyuanId":1, "shengao":175, "tizhong":70, "jianshenmubiao":"增肌", "jianshendengji":"中级", "meizhouxunliantian":4}`，模拟网络延迟60秒
**预期结果**  后端在30秒连接超时后自动降级（不等到60秒），切换至规则引擎生成方案；前端在AI调用期间显示加载动画/进度条，降级后正常展示生成的计划，并显示"AI服务繁忙，已为您生成基础方案"等友好提示；规则引擎生成的计划包含完整的7天训练安排（4天训练+3天休息）；数据库正确写入备用计划数据
**实际结果**  

---

## 测试结论（AI故障转移测试）

GLM-4-Flash API超时后，系统在______秒内切换至规则引擎，前端______显示友好提示，生成的备用计划数据完整性______，基础业务逻辑______被中断。

---

## 四、资源占用测试

---

**用例编号**  ZY-01
**用例名称**  CPU与内存资源占用监控测试
**测试目的**  验证系统在100并发用户正常负载下，后端服务的CPU使用率不超过20%，JVM堆内存使用量在合理范围（<512MB），满足性能准则中的资源占用要求
**测试方法**  使用JMeter维持100并发混合业务负载30分钟（同BF-01业务场景），同时使用VisualVM对JVM进程进行全量监控，每5秒采样一次CPU使用率和堆内存占用量（包括Eden区、Survivor区、Old区）；使用 `jstat` 命令辅助记录GC情况。负载结束后导出CPU和内存使用趋势图
**测试数据**  100并发混合业务场景，持续30分钟，VisualVM监控采样间隔5秒
**预期结果**  JVM进程CPU使用率平均值 < 20%，峰值 < 40%；堆内存使用峰值 < 512MB；Young GC频率 ≤ 2次/分钟，单次暂停 < 50ms；Full GC频率 ≤ 1次/30分钟；内存使用趋势呈锯齿状（有规律GC回收），无持续增长（无内存泄漏）
**实际结果**  

---

## 测试结论（资源占用测试）

100并发负载下，CPU使用率平均值为______%，JVM堆内存峰值为______MB，内存趋势______泄漏迹象。______于资源占用的性能准则要求。

---

## 五、7×24小时稳定性测试

---

**用例编号**  WD-01
**用例名称**  24小时连续运行稳定性测试
**测试目的**  验证系统在长时间连续运行中的稳定性，确保具备7×24小时持续运行能力，无服务中断、无内存泄漏、响应时间无持续退化
**测试方法**  使用JMeter设置50个线程持续循环混合业务请求，运行24小时。混合场景包含：会员查询（30%）、器材列表浏览（25%）、心得社区浏览（20%）、AI教练问答（15%）、教练信息查看（10%）。不包含AI计划生成以避免产生过高API费用。全程使用VisualVM监控JVM堆内存和CPU，每小时抽取一次响应时间样本（取每小时前5分钟数据），每小时记录一次数据库连接池状态、GC次数和线程数。测试结束后分析24小时内的服务可用性、错误率和性能趋势
**测试数据**  50并发用户循环24小时；数据库预置适当数据量（200会员、100器材、50心得）；每小时采样接口 `GET /huiyuan/page` 和 `GET /jianshenqicai/page` 的响应时间
**预期结果**  系统持续运行24小时无宕机、无手动重启；服务可用性 ≥ 99.9%（即累计不可用时间 < 86秒）；24小时总请求成功率 ≥ 99.9%；第24小时与第1小时的会员查询响应时间偏差 < 20%（无持续退化）；堆内存使用稳定在锯齿状波动状态，无持续上升（无内存泄漏）；Full GC频率 ≤ 1次/小时；数据库连接池活跃连接数稳定在10-15之间，无连接泄漏
**实际结果**  

---

## 测试结论（7×24小时稳定性测试）

24小时连续运行后，系统______宕机，服务可用性为______%，第24小时响应时间与第1小时相比______退化趋势，内存趋势______泄漏，数据库连接池______泄漏。

---

## 六、前端性能优化验证

---

**用例编号**  QD-01
**用例名称**  Vue路由懒加载与首屏性能测试
**测试目的**  验证前端Vue页面使用了路由懒加载优化（动态`import()`），各功能模块按需加载而非全量打包，确保在移动端、平板和PC浏览器上都能快速渲染
**测试方法**  分三步验证：（1）`代码审查`：检查 `front/src/router/router-static.js` 中路由定义是否使用 `() => import('@/views/modules/...')` 懒加载语法；（2）`首屏性能`：使用Chrome DevTools Lighthouse对登录后首页进行Performance审计，记录FCP（首次内容绘制）、LCP（最大内容绘制）和总bundle大小；（3）`三端渲染`：使用Chrome DevTools设备模拟器分别在PC（1920×1080）、平板（iPad 1024×1366）、手机（iPhone 12 Pro 390×844 + Fast 3G网络）上加载首页和会员列表页，评估渲染速度和布局自适应效果
**测试数据**  使用管理员账号登录；Lighthouse审计首页 `/home`；在三种设备模拟下分别访问首页和会员列表页
**预期结果**  路由配置使用懒加载语法，每个模块首次访问时独立加载chunk文件（chunk大小 < 200KB）；Lighthouse Performance评分 ≥ 70分，FCP < 2秒，LCP < 3秒，总bundle大小 < 2MB；PC端渲染流畅无卡顿；平板端左侧菜单栏自动折叠，表格自适应收缩；移动端首屏3秒内可渲染完成，布局为单列无水平溢出，按钮和表单元素适合手指操作（≥44px点击区域）
**实际结果**  

---

## 测试结论（前端性能优化验证）

Vue路由懒加载配置______，Lighthouse首屏评分为______分，FCP______秒，PC/平板/移动三端渲染______。______于前端性能优化要求。

---

## 附录

### 性能测试环境配置

| 配置项 | 参数值 | 说明 |
|:--|:--|:--|
| 测试服务器 | 4核CPU / 8GB RAM | 模拟生产环境 |
| JDK版本 | 1.8.0_202 | 后端运行环境 |
| JVM参数 | `-Xms256m -Xmx512m` | 堆内存配置 |
| MySQL版本 | 8.0.28 | 独立数据库 |
| Tomcat配置 | `max-threads=200, min-spare-threads=10, accept-count=100` | 内嵌容器线程池 |
| JMeter版本 | 5.5 | 负载生成工具 |
| 监控工具 | VisualVM 2.1 | JVM/系统监控 |
| 前端测试浏览器 | Chrome 110+ | Lighthouse审计 |

### 性能测试执行优先级

| 优先级 | 测试类别 | 用例编号 | 预计耗时 |
|:--|:--|:--|:--|
| P0 | 响应时间测试 | XT-01 | 30分钟 |
| P0 | 并发性能测试 | BF-01 | 1小时 |
| P1 | AI故障转移测试 | GZ-01 | 1小时 |
| P1 | 资源占用测试 | ZY-01 | 1.5小时 |
| P2 | 前端性能优化验证 | QD-01 | 1小时 |
| P2 | 7×24小时稳定性测试 | WD-01 | 24小时 |

---

> **文档结束** — 本文档共包含6个典型性能测试用例，分别对应响应时间、并发处理、故障转移、资源占用、长期稳定性和前端优化六大性能准则。测试执行后请在各用例"实际结果"栏和"测试结论"段中填写实测数据。
